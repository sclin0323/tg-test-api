app:
  auth:
    google:
      mock: false   # 本地測試不需要真實 Google OAuth
    mock:
      enabled: true                    # true = Demo 模式，token 寫死不碰 DB
      token: demo-token-2024           # 固定 token 值
      username: demo_user
      display-name: Demo 使用者
      email: demo@example.com
      role: ADMIN


spring:
  datasource:
    url: jdbc:postgresql://junction.proxy.rlwy.net:38143/railway
    username: postgres
    password: RsaZZLRGfilVdEJJMhvIdIQoGYNueRdF
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.the101: DEBUG